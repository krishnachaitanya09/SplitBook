<UserControl
    x:Class="SplitBook.Controls.PageHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SplitBook.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    VerticalContentAlignment="Center"
    VerticalAlignment="Center"
    HorizontalAlignment="Stretch"
    HorizontalContentAlignment="Stretch"    
    mc:Ignorable="d"
    Height="50"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="grid" Background="{x:Bind Background}">
        <Grid x:Name="titleBar">
            <ContentPresenter VerticalAlignment="Stretch" VerticalContentAlignment="Stretch" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" x:Name="content"
                              Margin="{x:Bind Padding}"
                              Content="{x:Bind HeaderContent}"/>
        </Grid>
    </Grid>
</UserControl>
